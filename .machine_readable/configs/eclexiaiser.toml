# eclexiaiser manifest — energy/carbon resource budgets
# SPDX-License-Identifier: MPL-2.0

[project]
name = "systemet"

# No [[functions] entries: systemet is a theory repo with no runnable code
# (the template's src/interface/ffi seam was removed). Add entries only if
# code-bearing artefacts ever land here.

[carbon]
provider = "static"
region = "GB"
static-intensity = 200.0

[report]
format = "text"
include-recommendations = true
